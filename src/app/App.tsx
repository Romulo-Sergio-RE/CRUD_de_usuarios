import React from 'react';
import { Rotas } from './routes';
import { GlobalStyles } from './GlobalStyled';

export const App = () => {
  return (
    <Rotas>
      <GlobalStyles />
    </Rotas>
  );
}

